<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ==================== PROPERTIES ==================== -->
    <!-- Fallback to 'logs' if env var is not set -->
    <property name="LOG_DIR"        value="${API_LOGGING_PATH:-logs}" />
    <property name="APP_NAME"       value="co-app" />
    <property name="MAX_FILE_SIZE"  value="${LOG_MAX_FILE_SIZE:-10MB}" />
    <property name="MAX_HISTORY"    value="${LOG_MAX_HISTORY:-30}" />
    <property name="TOTAL_SIZE_CAP" value="${LOG_TOTAL_SIZE_CAP:-1GB}" />

    <!-- ==================== PATTERNS ==================== -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} | traceId=%X{traceId} userId=%X{userId} | %msg%n" />

    <!-- ==================== CONSOLE APPENDER ==================== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- ==================== ROLLING FILE APPENDER ==================== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Active log file: co-app.log -->
        <file>${LOG_DIR}/${APP_NAME}.log</file>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--
                Rolled file naming: co-app-1.log, co-app-2.log, ...
                %i is the incrementing index (1, 2, 3...)
                .gz compresses rolled files automatically
            -->
            <fileNamePattern>${LOG_DIR}/${APP_NAME}-%i.log.gz</fileNamePattern>

            <!-- Roll when file reaches this size -->
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>

            <!-- Number of rolled files to keep -->
            <maxHistory>${MAX_HISTORY}</maxHistory>

            <!-- Hard cap on total log directory size -->
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ==================== ASYNC WRAPPERS ==================== -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>true</includeCallerData>
    </appender>

    <appender name="ASYNC_CONSOLE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="CONSOLE" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>

    <!-- ==================== LOGGERS ==================== -->
    <logger name="com.yourcompany" level="DEBUG" additivity="false">
        <appender-ref ref="ASYNC_CONSOLE" />
        <appender-ref ref="ASYNC_FILE" />
    </logger>

    <root level="INFO">
        <appender-ref ref="ASYNC_CONSOLE" />
        <appender-ref ref="ASYNC_FILE" />
    </root>

</configuration>
